apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: users
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
  - http:
      paths:
      - path: /api
        backend:
          serviceName: users
          servicePort: 80
